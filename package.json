{
  "name": "todos",
  "workspaces": [
    "todos-backend"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "analyze": "npm run eslint:check && npm run prettier:check",
    "eslint:check": "eslint */lib",
    "eslint:fix": "eslint --fix */lib",
    "prettier:check": "prettier --check .",
    "prettier:fix": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.14.0",
    "prettier": "^2.6.2"
  }
}
